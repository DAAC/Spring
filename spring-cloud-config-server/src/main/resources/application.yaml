spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DAAC/Spring/tree/master/limit-service
server:
  port: 8888